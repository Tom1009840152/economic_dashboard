# 经济学看板待办清单

更新日期：2026-09-16

状态说明：`待排期` / `进行中` / `已完成` / `受阻`。工作量为粗略估算：S（半天内）、M（1—3天）、L（3—7天）。优先级由项目负责人确认，编号保持稳定，便于后续讨论和提交记录引用。

## 数据与经济学口径

| 编号 | 状态 | 工作量 | 待办 | 验收标准 |
| --- | --- | --- | --- | --- |
| D1 | 已完成 | L | 建立真正的 `as_of` 历史数据视图 | 可按“截至某一时刻”重建每个观察期当时可见的最新版本；严格模式排除未知发布时间；接口支持日期范围和数量上限；修订与未知发布时间有测试。 |
| D2 | 已完成 | M | 统一分析层派生指标公式 | 信用脉冲、财政脉冲和 M1-M2 剪刀差只有一个公式实现与版本；按完整日历对齐，缺月/缺季度不跨期误算；存库结果记录公式版本。 |
| D3 | 已完成 | M—L | 补齐关键数据缺口和最新月份 | 优先补人民银行社融增量/存量及分项、政府债券融资、地方专项债和库存周转；记录实际可得区间，无法补齐的缺口不得伪造。 |
| D4 | 已完成 | M | 完善指标数据字典 | 每个指标都有来源、频率、度量类型、季调状态、累计/单期属性、模型方向、推荐变换、发布滞后、硬边界与口径备注；启动或测试时自动校验。 |
| D5 | 已完成 | M | 建立数据质量门禁 | 检查空数据、重复日期、未来日期、非数值、行数突降、异常跳变、合理区间、发布元数据及关键序列交叉一致性；明显坏数据不得覆盖最后成功快照。 |
| D6 | 已完成 | M | 保存刷新运行台账 | 每次刷新记录触发方式、起止时间、总状态；逐指标记录成功/无变化/失败、输入行数、版本变化数、耗时与错误；可查询最近状态。 |
| D7 | 已完成 | L | 补齐周期输入的历史发布证据（首轮） | 从可核验的官方公报或归档补录原始发布时间与当时值；不得用经验发布日期或当前最终值倒填；至少让两个同步板块连续 24 个月可严格回放。 |
| D8 | 待排期 | M | 统一 `available_at` 时区标准 | 新写入统一为带来源时区后转换的 UTC，迁移并核验存量无时区时间；跨来源的小时级 as-of 比较不再依赖地区假设。 |
| D9 | 进行中 | L | 扩充严格回测的来源链与历史基准期 | 信用、房地产、财政、消费者和通胀来源链已完成；剩余重点是等待并 canary 监测海关官方美元传输、完成 A3 strict-only 审计，并继续向 2021 年以前延伸可核验基准。A4 仍须通过主动样本、阶段覆盖和转换数三道门。 |

## 中国经济周期分析

| 编号 | 状态 | 工作量 | 待办 | 验收标准 |
| --- | --- | --- | --- | --- |
| A1 | 已完成 | L | 建立模型专用月度活动矩阵 | 按五个固定等权板块组织同步与领先信号；完成组内去重、单边标准化、时间对齐、缺失门槛、公式版本隔离、覆盖率与贡献审计，并标记成分变化造成的环比不可比月份。 |
| A2 | 已完成 | L | 实现中国经济周期定位 | 输出复苏、扩张、放缓、收缩四阶段；通胀作为独立维度；同时输出数据截止日、本期证据质量和主要贡献指标。 |
| A3 | 已完成 | L | 做伪实时滚动回测 | 每一期只使用当时已发布的数据；保存阶段判断、后验结果和误判情况；可比较实时值与修订后值。 |
| A4 | 待排期 | M—L | 输出阶段转换概率 | 输出未来3、6、12个月转阶段概率与区间，不承诺单一确定日期。 |
| A5 | 进行中 | M | 增加分析可解释性 | 展示本期判断由哪些指标推动或拖累，以及结论较上个可比月改变的原因；严格区分模型内贡献与经济因果。 |
| A6 | 待排期 | M—L | 重做通用预测体系 | 通用 ARIMA 明确标记实验性；周期指标改用滚动训练、单边变换和适合各类频率/累计口径的模型。 |
| A7 | 已完成 | S—M | 统一不可比月的事后参考路径 | 逐月表中的事后参考都按同观察端点精确重跑或明确隐藏；不再让完整终点路径承担不可比月的展示参照。 |
| A8 | 已完成 | S—M | 拆分主动判断与阶段沿用 | `confirmed/transition` 与 `stale/held_uncomparable` 分开统计；陈旧阶段展示上次确认阶段、确认时间、最近可决策月和沿用时长，不再把长期沿用标签表述成当月新判断。 |

## 后端与运行可靠性

| 编号 | 状态 | 工作量 | 待办 | 验收标准 |
| --- | --- | --- | --- | --- |
| R1 | 待排期 | M | 优化国家指标列表查询 | 不再逐指标懒加载完整历史；一次国家列表查询只取最新两期和近30个绘图点，并有性能测试。 |
| R2 | 待排期 | M—L | 拆出单实例刷新任务 | 多 Web worker 不会重复启动调度器；刷新具有互斥、超时和安全退出机制。 |
| R3 | 待排期 | S—M | 保护手动刷新接口 | 生产环境默认禁用或要求认证；请求异步返回任务编号，不长期占用 Web 请求。 |
| R4 | 待排期 | M—L | 页面只读取最后成功快照 | 人口、就业和分析页不因上游临时故障不可用；上游采集进入统一入库和版本流程。 |
| R5 | 进行中 | M | 限制历史、版本及预测接口 | 支持日期范围、分页/点数上限、缓存和超时；伪实时回测已有进程内结果缓存、同键合并和数据水位失效，后续补复合索引、预计算及跨进程并发限制。 |
| R6 | 待排期 | M | 完善健康检查和一键启动 | 就绪检查覆盖数据库、迁移和最近刷新；提供统一启动前后端的开发命令。 |
| R7 | 待排期 | M—L | 建立 CI 与部署流程 | 自动执行 lint、类型检查、构建、后端测试、迁移测试和依赖扫描，并形成可重复部署流程。 |

## 前端体验

| 编号 | 状态 | 工作量 | 待办 | 验收标准 |
| --- | --- | --- | --- | --- |
| U1 | 待排期 | M | 增加加载、错误和重试状态 | 网络错误、上游不可用、真正无数据和404分别呈现；关键路由有 loading/error UI 与局部重试。 |
| U2 | 待排期 | M | 展示每项分析的真实时点 | 每个分析信号显示观察期、发布日期、可用时间、来源、版本和陈旧程度；总截止日取最慢关键输入。 |
| U3 | 待排期 | M | 按指标频率缓存页面 | 日/月/季数据使用不同缓存策略；二级 Tab 切换不重复拉取未变化数据。 |
| U4 | 待排期 | S—M | 导航由实际能力驱动 | 空栏目隐藏、禁用或明确显示建设状态；国家能力由统一配置生成。 |
| U5 | 待排期 | M—L | 增加全局搜索和指标比较 | 支持按名称/代码搜索、收藏，以及多指标标准化或双轴比较。 |
| U6 | 待排期 | M | 优化移动端和可访问性 | 页面语言为中文；补齐键盘焦点、当前项语义、按钮状态、减少动画偏好和图表文本摘要。 |

## 覆盖扩展与维护

| 编号 | 状态 | 工作量 | 待办 | 验收标准 |
| --- | --- | --- | --- | --- |
| X1 | 待排期 | L | 补齐现有国家栏目 | 日、欧、英、韩的债券、货币和综合分析逐步达到可解释、可追溯的基础覆盖。 |
| X2 | 待排期 | L | 增加印度专题 | 完成增长、通胀、就业、货币、外部和市场的最小指标集及数据源评估。 |
| X3 | 待排期 | M | 清理重复代码和过期文档 | 统一国家配置、卡片与分类逻辑；文档中的地区定义、数据源和接口与代码一致。 |
| X4 | 待排期 | M—L | 扩大测试覆盖 | 覆盖采集契约、公式、API、六国路由、错误态、移动端导航和关键分析交互。 |

## 当前基线

- 指标定义与采集器：150 对 150，无漏配。
- 可见指标：96；中国周期底层指标：51。
- A3 固定截止 2026-07 的 120 个月回放截面：核心 CPI final/known/on-schedule 均为 115，PPI strict/on-schedule 均为 120，出口 strict 仍为 0。整体结果保持 93 个含沿用标签可比月、25 个主动判断月和 2 个匹配转换；含沿用标签一致 48/93，主动判断一致 13/25。固定调查有 86 个主动判断月、8 个匹配转换，但没有“放缓”阶段，A4 仍为 `NO-GO`。
- A1 `1.1.0` 为消费者预期预先固定一月观察时滞。A2 `1.1.0` 增加水平/动能逐指标精确分解和状态机变化原因，A3 `1.4.0` 明确主动判断、沿用时长、同端点事后参考和按月 `R/H/F` 模型差异审计；这些解释层改造不改变阶段路径。生产基线仍为含沿用标签一致 48/93、主动判断一致 13/25；固定调查诊断在共同月份分别提高到 71/93 与 19/25，但缺少“放缓”阶段，三道训练门槛未全部通过，不能替代生产模型或开放 A4。
- D7 首轮新增官方发布证据：非制造业商务活动 59 条、工业增加值 53 条、社零 50 条；回测截止月内分别有 58、53、50 条已知发布时间，并有 58、52、49 条在固定决策截止时点前可用。
- D9 当前新增：房地产三个核心累计同比各新增 62 个官方历史版本且 0 冲突；每条现有 237 个当前观测、116 个分钟级发布时间已知、114 个在固定决策时点前可用，严格证据范围延伸至 2016-02—2026-07。70 城新房环比上涨城市占比和环比中位数各新增 59 个公式版本化的官方历史版本，覆盖 2021-09—2026-07，其中 56 个月按时可用；2026-08 与当前最终值不一致，安全跳过。景气归档跨 `index_1000` 与 `index_2000` 分片延伸到 2016 年，`CN_NMI`/PMI生产/PMI就业各 125 条、工业增加值 114 条、社零 100 条发布时间已知。
- D9 财政证据升级新增独立、只追加的 `release_evidence` 表：国家统计局名义 GDP 首发 19 季，财政部一般公共预算支出 81 月、政府性基金预算支出 80 月，并派生广义财政支出 80 月、财政支出强度 19 季和财政脉冲 15 季，共写入 294 条；重复运行新增 0 条，财政回填当次目标环境的 331 条当前值快照及指纹未改变。后续对 `data_points` 的财政 current 缺口执行了独立、missing-only 修复：固定白名单仅含 2022-04—2022-12 与 2024-12，新增 current 42 条及对应 vintage 42 条，五项财政链合计由 253 条增至 295 条；二次 dry-run 为 0 写入。
- D9 消费者证据升级从 CEI 经验证分发页保存预期、满意和信心各128条、127个不同观察月（2015-12—2026-06），共新增384条，重复运行新增0条；该里程碑时 `release_evidence` 总量为678条。完整 `data_points` 写入前后均为213,509条且内容指纹不变。
- D9 人民银行信用证据升级覆盖15个代码，首次新增1,174条合格 `release_evidence`；相同参数复跑全部新增0条，聚合值与内容不变。该数量是信用链里程碑，不是当前全库总量。社融月度证据140期（2015-01—2026-08，直接发布88/累计差分52），社融口径人民币贷款127期（67/60），企业债124期（66/58），政府债80期（34/46，2020-01起）；社融与贷款存量同比各132期，M1/M2/M1-M2各32期，信用强度45期，信用脉冲连续33期。
- D9 通胀来源链已经完成生产升级。核心 CPI 以国家统计局月度 CPI 表为 canonical 来源，2017-01—2026-08 连续116/116；PPI 解读稿为2016-08—2026-08连续121/121，生产完整性门截至2026-07为 `ready=true`。正式写入核心 CPI 116条、PPI 121条合格 `ReleaseEvidence`；全库现有2,089条且全部 verified，出口仍为0。核心 CPI current missing-only 新增93个缺失键，23个重叠值全一致、冲突0，current/vintage最终为116/117；2025-02直接月表发布时间校正为2025-03-09 09:30，值为`-0.1%`，2025-06为`0.7%`。固定120个月回放仍为93个可比月、25个主动判断月、2个匹配转换，A4继续`NO-GO`。
- 通胀常规刷新闭环已完成。核心 CPI 近期月表实测13期（2025-08—2026-08）；PPI 近期官方解读稿实测17期（2025-04—2026-08），再与聚合源合并为248期长历史（2006-01—2026-08）。最新PPI为2026-08的`3.8%`，官方发布时间2026-09-09 09:30。两条近期详情都会实时重验；核心 CPI 拒绝未晚于观察月末的发布时间，PPI 拒绝“最新月只有聚合值、没有官方URL/时间”的返回。普通刷新只更新current/vintage，来源专用`ReleaseEvidence`仍由独立回填入口追加。
- 海关出口链仍为 `NO-GO`：真实美元初值域的 TLS 主机名和信任链都失败，online 域没有相应数据，ChinaPort 近期页面多为人民币口径，不能混入 `CN_EXPORTS`。系统没有关闭 TLS 校验或混用币种，合格证据保持0；后续仅等待官方传输、做 canary，或另行设计 formal derived 序列。
- 前端 ESLint、TypeScript 和生产构建通过。本轮后端完整测试424/424通过，`compileall`与差异格式检查通过；本地后端健康检查返回正常，中国综合分析页HTTP 200。测试项数量只属于各次验收快照，后续仍以最新完整输出为准。
- 2026-09-13 实测中国指标列表：32张卡产生33次 SQL 查询，约1.93秒。

## D1—D7 执行记录

- D1：新增 `/api/as-of/indicators/{code}/history` 与 `/api/as-of/snapshot`。严格模式只使用真实 `available_at`；非严格模式才以首次抓取时间兜底。批量快照分别报告未知指标和当时时点不可用指标。
- D2：信用强度、信用脉冲、财政支出强度、财政脉冲代理和 M1-M2 剪刀差统一到版本化公式服务；信用与财政派生指标当前为 `1.0.0`，采用人民银行新 M1 可比口径的 M1-M2 剪刀差为 `1.1.0`。信用和财政同比先补齐日历索引再移位，禁止跨缺口错配。
- D3：社融总量、人民币贷款及企业债月度增量覆盖 2015-01—2026-08；社融存量同比连续覆盖 2017-01—2019-12、2022-01—2026-08，社融口径人民币贷款存量同比的外沿区间相同，但缺 2025-11、2025-12、2026-01、2026-02；专项债当前库覆盖 2019-07—2026-07；工业库存周转天数覆盖 2025-06—2026-07。
- D4：150 个指标全部进入数据字典，并由 `/api/indicator-catalog` 提供查询；未登记来源或非法口径会在启动/测试阶段失败。
- D5：质量门禁在写入前执行，数据、版本和单指标台账同事务提交；异常跳变只告警，确定性结构错误与硬边界错误会阻止覆盖。
- D6：新增刷新运行及逐指标结果表，接口为 `/api/refresh/status`、`/api/refresh/runs` 和 `/api/refresh/runs/{run_id}`。已在真实数据库完成一次 `CN_TSF` 无变化刷新验证。
- D7：逐月读取国家统计局原发布页，将页面中的当时值、实际发布时间和来源 URL 一起写入版本库。新增 `activity` 归档组、可续跑详情页缓存、每次更新的索引读取、`--archive-pages` 和 `--check-only`；归档只为数据库中已经存在、且与官方原发布值精确一致的当前观测补元数据，不会依据历史页新建缺失的当前观测。审查修正了“1—N 月累计值误作单月值”的解析问题，最终 `CN_NMI` 59 条、`CN_IP` 53 条、`CN_RETAIL` 50 条均为 0 冲突。常规 `CN_NMI` 刷新保留 224 个月长历史，国家统计局近期页暂不可用时也不会坍塌。A1 两个同步板块可连续严格评分 34 个月，达到本项首轮验收线；D7 完成时的历史回放快照只有 5 个非连续含沿用标签可比月和 0 个主动判断可比月，这不是当前 D9 回放结果。

## D9 执行记录（进行中）

- 房地产原发布页兼容“全国房地产开发投资和销售情况/全国房地产市场基本情况”等历史标题，以及“商品房/新建商品房”“房屋/房地产新开工施工面积”等历史标签。2026-09-15 修复了国家统计局迁移旧页标题时间栏的解析，证据只接受 HTTPS 官方 `stats.gov.cn` 页面。销售面积、新开工和投资累计同比各新增 62 个历史版本且 0 冲突，重复执行均为 0 新增；每条序列现有 237 个当前观测、116 个月分钟级发布时间已知、114 个月在固定决策时点前可用，严格证据范围为 2016-02—2026-07。2018-12 在 2019-01-21 发布、2022-09 在 2022-10-24 发布，均晚于固定决策时点，按真实发布历史保留，不人工调整。
- 国家统计局数据发布目录按每 1000 条分片：现行分片止于 2021-09，`index_1000*` 接续至约 2017-05，`index_2000*` 再向前。归档脚本新增 `--archive-shard` 与 `--archive-start-page`，可小批续跑；2018 年以前制造业 PMI 与非制造业商务活动分开发布，采集器已兼容两类原页面。
- 当前严格发布时间覆盖：`CN_NMI`、PMI生产、制造业/非制造业就业各 125 期；工业增加值 114 期；社零 100 期。所有写入必须是 HTTPS 官方域名、分钟级真实时间、且与库内当前值在六位小数精度下完全一致；除 `published` 原始值外，派生证据必须同时匹配指标代码、公式版本和发布机构官方域名：央行累计差分只接受 `pboc_ytd_diff_v1`，70 城上涨占比和环比中位数分别只接受 `nbs_70city_rising_share_v1` 与 `nbs_70city_mom_median_v1`。历史模式缺少专用证据时直接跳过，绝不回退普通抓取器。重复执行最后一批全部为 0 个新版本。
- 70 城归档只读取国家统计局发布页“表1”的新建商品住宅价格环比指数，并要求“上月=100”、完整 70 个唯一城市、观察月与标题一致；二手房、同比、面积分档、残表和非官方域名全部拒绝。上涨城市占比和环比中位数各写入 59 个官方版本，范围为 2021-09—2026-07；其中 56 个月赶上固定决策时点，2022-01、2022-09、2024-01 按真实延迟排除。2026-08 两项聚合值与当前最终值不一致，0 覆盖、0 误写。固定 2026-07 截面的配对重算中，房价证据把领先共同覆盖从 50% 提高到 56.67%，但仍低于 65% 门槛，阶段保持收缩且领先方向继续不可用。
- 人民银行完整信用证据链已经进入独立、只追加的 `ReleaseEvidence`，不再要求首发值与今天的亿元精确当前值完全一致。15个代码包括累计与月度社融、社融口径人民币贷款、企业债、政府债，社融与贷款存量同比，M1/M2/M1-M2、信用强度和信用脉冲。月度正文没有直接值时，用相邻两篇首发稿按0.01万亿元披露的累计值计算发布残差，标记 `pboc_ytd_diff_v1` 并保留两端来源；它不是人民银行直接公布的精确单月值，也不写入 `data_points` 当前/修订层。信用强度与信用脉冲的 `available_at` 取全部社融和名义GDP叶子的最晚时刻。2024新口径M1可比表只从人民银行2025-02-14 17:00原发布页起可用，不倒填到各观察月。
- 信用回填命令默认为只读预检：`.\.venv\Scripts\python.exe -m scripts.backfill_china_credit_evidence`；只有追加 `--apply` 才会写入。门禁要求社融月度自2015-01连续、M1-M2自2024-01连续、最近24个月分项和存量链完整、信用脉冲连续且不少于24期，并检查最新月份；整批原子写入，复跑0新增，且不得改变 `data_points`。
- 名义 GDP 与财政链不再依赖“当前值倒填首发时间”。新增独立、只追加的 `ReleaseEvidence`：国家统计局季度名义 GDP 首发 19 期（2021Q4—2026Q2），财政部一般公共预算支出 81 期（2019-04—2026-07）、政府性基金预算支出 80 期，并按 `广义支出=一般公共预算支出+政府性基金预算支出`、`支出强度=广义支出/同期累计名义GDP×100`、`财政脉冲=本季强度−上年同季强度` 生成广义支出 80 期、强度 19 期和脉冲 15 期（2022Q4—2026Q2）。派生版本的发布时间取全部叶子输入中的最晚时刻，财政脉冲保留六个叶子及来源。完整性门要求 2021Q4 至最新应发布季度连续，残缺抓取在打开数据库前拒绝；真实库首次写入 294 条、重复运行 0 条，财政回填当次目标环境的 331 条当前值快照内容指纹不变。
- 财政 current 缺口修复与上述首发证据回填严格分离。修复入口默认 dry-run，只对 2022-04—2022-12、2024-12 的两项直接财政叶子开放；每次强制实时深扫财政部 10 页索引及详情页，索引与详情都禁止缓存回退，并要求新抓官方值、合格 `ReleaseEvidence`、已有 current 三方在六位小数下无冲突。派生值只用修复后的 current 财政叶子和 current 名义 GDP 按统一公式重算，不复制派生首发证据。实际新增一般预算支出、基金预算支出、广义支出、支出强度、财政脉冲分别 10/10/10/4/8 条，共 42 条 current 与 42 条 vintage；五项 current 合计 253→295，复跑 dry-run 计划写入 0 条。
- 核心 CPI 的 canonical 来源已切换到国家统计局月度 CPI 数据表。解析器只读取“其中：不包括食品和能源”行及两种受控同比表头，逐页校验请求/最终 URL、完整官方 HTTPS 重定向链、可见分钟和缓存版本；2017-01—2026-08 连续116/116。常规六小时刷新也使用该月表的近期滚动窗口，不再解析解读稿核心 CPI；坏详情、空结果、非分钟级时点及未晚于观察月末的发布时间均 fail-closed，近期详情会实时重验。PPI 解读稿独立覆盖2016-08—2026-08共121/121；常规 PPI 刷新已改为近期官方解读稿与 AkShare 长历史合并，官方行覆盖重叠月份，聚合源若独有最新月则整批拒绝。两条生产门截至2026-07均为`ready=true`。2025-02直接月表为`-0.1%`、发布时间2025-03-09 09:30，2025-06为`0.7%`。普通刷新只维护current/vintage，`ReleaseEvidence`仍由独立回填入口追加。
- 核心 CPI current 补全与首发证据写入严格分层。入口默认 source-only，不打开数据库；`--check-current` 只读，`--apply` 才在单事务中重建计划。23个已有键在六位小数下全一致且metadata保持不变，只写93个缺失键，状态为`historical_backfill`，current/vintage最终为116/117。首次apply因生产`SessionLocal`使用`autoflush=False`，pending vintage未进入数量查询，计数门触发并完整回滚；计数前显式`flush`后成功。成功写入后的生产库复检为116个重叠月份全一致、冲突0、待新增0。该流程不会覆盖已有current，也不会把首发值冒充最终值。
- 海关总署英文初值链只接受 HTTPS 官方域、全国美元总值单月同比；1—2月合并稿不造单月值，日期级页面保守记为次日 00:00 的 `date_upper_bound`，连续窗口固定自2020-03。真实美元初值域同时存在 TLS 主机名与信任链失败；证书有效的online域没有相关数据，ChinaPort近期页面多为人民币口径，不能混入`CN_EXPORTS`。采集器继续fail-closed，0条入库；后续只等待官方传输、做canary，或另建formal derived序列。
- 消费者调查从 `www.cei.cn` 与 `ibe.cei.cn` 目录发现候选，逐页校验标题月、页面日期、来源标签“国家统计局”、唯一四列表、数值范围和三序列连续性；CEI 明确记为分发镜像，不冒充统计局官网。页面只有日期，故使用次日 00:00 的保守 `date_upper_bound`；滚动旧行只证明在更晚页面日期已经可知，绝不倒填。三项各128条/127月，2019-04保留两版，共写入384条，复跑新增0条。完整性门要求从2015-12无缺月并至少更新到当前月减3个月。消费者信心两种60/40关系只作非阻断诊断，不把未公开的固定权重当真实性门。
- 固定 `end=2026-07` 的 120 个月真实回测，从 D7 完成时的历史快照（5 个阶段可比月、0 个主动判断月）提高到当前 D9 的 93 个含沿用标签可比月、25 个主动判断可比月。消费者链里程碑时，4,574个当前公式最终观测中有1,985个可还原、1,896个按模型目标月可用，严格输入代码为19/23；人民银行信用链进一步提高到2,036、1,946和20/23。信用块因24个月校准预热从2026-01起完整工作，2026-07严格领先覆盖与口径覆盖均达到100%，领先指数95.01（最终值95.52）。新增信用证据没有改变任何阶段标签：含沿用标签一致仍为48/93（51.61%）、主动判断一致13/25（52.0%），水平轴一致率91.89%，动能轴一致率75.68%，匹配转换仅2次。严格阶段只有收缩81个月、扩张12个月，主动判断只有收缩21个月、扩张4个月；四阶段样本仍不完整。剔除2020年后含沿用标签一致37/81（45.68%），主动判断一致10/21，后者不足24个月所以不展示比率；A4继续 `NO-GO`。
- A3 升级为 `1.3.2`：`1.2.0` 引入独立证据优先，`1.3.0` 新增 `evidence_kind/chain_verified/availability_precision` 资格门，只有核验链和受支持精度的证据才会排除同键通用版本；`1.3.1` 让输入就绪率按信号的模型目标月统计；`1.3.2` 再让就绪率分母和版本冲突审计仅使用 A1 当前要求的公式版本，旧口径仍保留在库中但不再稀释当前口径覆盖率。迁移 `a61d7c04e932` 只把明确六个财政/GDP代码、合格状态及官方域名的既有294条标为 `official_release/true/exact_minute`；直接插入默认 `unclassified/false/unknown`，降级再升级不会把CEI镜像误标成精确官方原稿。
- 发布证据支持两种显式时间精度：`exact_minute` 必须与发布日同日，`date_upper_bound` 必须严格等于发布日次日00:00；裸日期、未知精度和未核验链不能取得权威优先级。D8完成前仍拒绝带时区时间，防止直接剥离偏移量造成前视。后续P2还需让来源/血缘元数据可订正，并评估完整性门在季度发布延迟时的安全误报；这些边界只会拒绝写入，不会污染现有证据。
- 固定调查篮子 `fixed_survey_core_v1` 只包含 PMI 生产、非制造业商务活动、制造业就业、非制造业就业，四项各25%，连续六个月任一项缺失即停判，不动态补权。它在自身样本上含沿用标签一致 73/96（76.04%）、主动判断一致 71/86（82.56%）；与生产基线完全相同月份配对后分别为 71/93（76.34%，较基线 +24.73 个百分点）和 19/25（76.0%，+24.0 个百分点）。这说明动态篮子/硬数据缺口是当前不稳定的重要来源，但该篮子按完整生产宇宙只有60%覆盖、且没有出现“放缓”阶段，只能是 `diagnostic_only`，A4 继续 `NO-GO`。
- 仅从评价分母删除1月后，含沿用标签一致为 44/85（51.76%，+0.15个百分点），主动判断一致仍为 13/25（52.0%，+0.0个百分点）。这不是反事实重算，也没有消除1月缺口对随后六个月共同篮子的影响；结果说明问题不能靠隐藏1月解决。
- 升级后的完整回放冷算实测 313.654 秒；同区间第二次命中 R5 缓存为 0.551 秒，约快570倍。缓存为每进程8项 LRU、15分钟 TTL，相同键只构建一次；正常版本化写入按当前值/版本表水位立即失效，异常原地维护的陈旧上限为15分钟。多 worker 各自冷建、复合索引和中间结果预计算仍属 R5 后续。

明确保留的资料缺口：`data_points` 当前/修订层中的两项社融存量同比仍保留原有缺月，不用首发新闻值冒充后来修订后的当前值；与之独立的人民银行首发证据层各已有132期，用于回答历史决策时点“当时知道什么”。库存周转天数在当前可稳定解析的官方发布归档中仅从 2025-06 开始。普通通胀刷新尚不自动追加 `ReleaseEvidence`，需要把现有独立回填入口接入单实例任务后再关闭这一运维缺口。直接冷启动导入 `app.fetchers.macro_source` 仍有既有循环依赖，生产总注册表路径不受影响，但应在 X3 清理。多进程刷新互斥仍属于 R2，不计入 D1—D7。

## A1 执行记录与旧库部署顺序

- A1 已提供 `GET /api/analysis/cn/business-cycle/matrix` 与中国综合分析页入口，当前方法版本为 `1.1.0`。主分数使用五个固定 20% 基础权重板块；月度信号至少使用此前 24 个观测、季度信号至少使用此前 8 个观测，在最多 60 个月的单边窗口内计算稳健标准分。消费者预期在源序列完成标准化后固定滞后一月用于模型目标月，并保留真实 `source_period`。
- 工业增加值历史由国家统计局近期发布与东方财富传输镜像合并；M1-M2 只使用 `1.1.0` 及 2024-01 以后的可比口径；旧公式记录保留在版本库中，但不与现行口径拼接。接口分别报告输入到齐率、有效板块覆盖率和已知发布时间覆盖率，并在有效信号集合或归一化权重变化时禁止直接展示环比结论。
- 旧数据库中的 `CN_GDP` 曾把累计季度值记在 1/4/7/10 月。升级时必须先暂停后端调度器及其他写入者，部署迁移脚本但不要启动包含新版采集器的 Web 进程，然后在 `backend` 目录依次执行只读预检和带唯一备份文件的迁移：

  ```powershell
  .\.venv\Scripts\python.exe scripts\realign_cn_gdp_quarters.py
  .\.venv\Scripts\python.exe scripts\realign_cn_gdp_quarters.py --apply --backup ..\..\economic-dashboard-backups\cn-gdp-before-realign-YYYYMMDD-HHMMSS.json
  ```

  备份目录应放在 Git 仓库之外，避免真实数据库快照被误提交。

  迁移会在同一事务中把当前值及关联版本记录移动到 3/6/9/12 月。确认输出为 `applied` 或 `no_change`，且当前值和版本记录均只落在季度末月份后，才能启动新版后端和自动刷新。若目标季度末已存在记录，脚本会拒绝模糊合并；不得绕过检查，应先核对冲突来源。当前项目数据库已于 2026-09-13 完成该迁移。

## A2 执行记录

- A2 新增 `GET /api/analysis/cn/business-cycle/regime`、中国综合分析首张“经济周期定位”卡和 `/analysis/cn/business-cycle` 详情页；A8 后方法版本为 `1.0.1`，A5b 完成后为 `1.1.0`，当前阶段有效期与历史阶段分离后为 `1.1.1`。
- 同步和领先分别采用六个月共同成分平衡面板，用相同信号与权重重算“最近三个月”和“此前三个月”；两轴均设 ±1.5 点死区，输出复苏、扩张、放缓、收缩四象限。相邻象限允许双向确认，对角候选至少连续 3 个可决策月，切换不回填。
- 共同篮子变化月展示平衡值但暂停阶段判定，并重置未确认候选，避免跨口径拼接连续证据；连续不可决策会明确标记为沿用/陈旧。领先只负责缩短或延长候选确认，不会阻断同步阶段判断；相同截止月始终用最多 240 个月历史预热，避免摘要页与详情页因展示窗口不同而得出不同状态。当前真实数据近 24 个月中有 18 个月可参与判定（75%），最新数据月与最近可判定月分别展示。
- 详情页同时展示确认状态与候选进度、周期罗盘和死区、阶段时间线、同步水平支撑/拖累、PMI 绝对荣枯锚、核心 CPI/PPI 通胀环境、置信度和改变判断的条件。最终值历史不冒充当时实时判断；历史信息可得性与修订稳定性由 A3 独立检验。

## A3 执行记录

- A3 新增 `GET /api/analysis/cn/business-cycle/backtest` 与 `/analysis/cn/business-cycle/backtest`。每个观察月 `m` 固定在次月 20 日 18:00（北京时间）截取信息，观察期锁在 `m` 月末；默认和最大区间均为 120 个月。
- 严格模式只接受 `available_at` 已知且不晚于决策时点的版本；未知发布时间、未来发布、发布时间不递增的修订，以及改值但修订发布时间未知的版本不会进入回放。数值未变的稀疏刷新仍可保留较丰富的原发布元数据；数值改变时不得继承旧发布时间。
- 当时判断和事后参考都直接复用现行 A1 计算与 A2 状态机，并按同一观察月比较。事后参考只是“今天的最终快照用同一模型重算”的稳定性参照，不是 GDP 真值、官方衰退认定或预测准确率。
- 只有两侧都有阶段标签的月份才进入含沿用标签一致率与翻转率分母；主动判断稳定率另只统计两侧当月均可判且已有确认阶段的月份。任一口径少于 24 个可比月时比率返回 `null`，少于 5 次匹配切换时确认滞后返回 `null`。2020 年保留在主样本，并另给出剔除 2020 年的敏感性结果；任一侧样本不足时不比较。
- D9 当前回放固定为2016-08—2026-07共120个月。核心 CPI final/known/on-schedule均为115，PPI strict/on-schedule均为120，出口strict为0。含沿用标签可比月93个、主动判断可比月25个；含沿用标签一致48/93（51.61%）、主动判断一致13/25（52.0%），水平轴和动能轴一致率分别为91.89%与75.68%，匹配转换仅2次。固定调查有86个主动判断月、8个匹配转换，但没有“放缓”阶段。通胀链补齐提高了来源完整性，没有改变阶段标签；结果只衡量模型对历史版本的稳定性，不能解释为经济预测准确率，A4仍为`NO-GO`。
- A3 `1.4.0` 保留 `1.1.0` 预注册的 `fixed_survey_core_v1` 诊断：固定四项调查信号、各25%，复用同一次 A1 标准化结果，只额外运行 A2 状态机；领先篮子和确认规则不变。接口同时给出完整样本、剔除2020、固定篮子、仅删除1月评价行，以及相同月份配对比较、阶段分布和“主动样本≥24/四阶段齐全/匹配转换≥5”三道门槛。固定诊断虽明显更稳定并有8次匹配转换，但缺少放缓阶段；不能据此替换生产模型。
- 删除1月场景明确是 `evaluation_window_only`：它不重算路径，不把12月与2月拼成连续转换，也不声称消除了结构性缺口。真实结果只改善0.15个百分点，主动判断一致率没有变化。
- 全库版本链审计未发现中国周期指标存在“数值修订复用旧 `available_at`”或发布时间倒退。若数值或公式发生变化但修订公开时点不可证，A3 会隔离该观察期的全部通用版本，而非继续使用可能已失效的旧值。通用版本库中 `CN_CREDIT_IMPULSE` 的8条未知时间修订仍保留审计，但2023-12以后的模型区间已由独立人民银行证据链覆盖；`CN_M1M2` 现行口径在回测截止内31/31期均有严格证据。名义GDP、财政脉冲、消费者预期、完整信用块及核心 CPI/PPI 的有序证据已经完成；来源专用 `ReleaseEvidence` 中核心 CPI为116、PPI为121、出口为0。D9下一优先级是出口官方传输canary、A3 strict-only审计、阶段敏感性及历史基准扩展；D8和R5继续按独立待办推进。不能因最新领先覆盖过线就直接训练A4。

## A7 执行记录

- A3 升级至 `1.3.4`。逐月事后参考不再从回测结束月的一次完整状态机路径中抽取早期标签：只要生产基线或固定调查诊断在当月形成可展示标签，就用今天最新值按同一观察端点、同一240个月预热窗口精确重跑；两侧都未形成标签时，逐月事后参考明确隐藏。
- 每个月新增 `final_reference_status`：`same_endpoint_rerun` 表示展示结果来自同端点精确重跑，`hidden_no_realtime_label` 表示因当时没有可比较标签而主动隐藏，`same_endpoint_unavailable` 表示已要求同端点重跑但无法形成同月结果。策略隐藏不再误报为“事后同月缺失”。
- 删除完整终点路径及其 `last_decision_period` 视图回退，避免 A2 的确认阶段、候选连续月数和不可判连续月数从错误的状态机起点继承。该改造不增加逐月精确重跑次数，并省去一次完整最终路径预计算。
- 固定2016-08—2026-07的120个月真实重算中，96个月展示同端点精确结果、24个月明确隐藏、0个月同端点不可用，且不存在“非同端点状态却带 final 标签”的记录。原有93个含沿用标签可比月、25个主动判断可比月及其稳定率、转换、敏感性和输入准备度统计全部保持不变。
- 页面把事后参考标为“同端点”，对隐藏与同端点不可用使用不同提示，并明确说明不再借用完整终点路径填充不可比月份。

## A8 执行记录

- A2 `1.0.1` 为每个月增加 `phase_basis`、`carry_forward_months` 和逐月 `last_decision_period`。`active_decision` 表示本月输入可判且已有有效阶段，`pending_confirmation` 表示只形成候选，`carried_forward` 表示本月不可判而沿用旧阶段，`unclassified` 表示仍无可展示判断；`duration_months` 继续表示阶段年龄，不再被误作沿用时长。
- 摘要卡、详情页和逐月回测均把“本月暂不可判”作为沿用状态主标题，旧阶段只显示为“上次确认”，并同时展示确认月、最近可判月和连续未更新月数。候选方向明确标为尚待连续确认，不冒充已确认阶段。
- A3 `1.3.3` 不改变既有阶段路径或 `decision_*` 算法，只把93个可比月拆成互斥口径：25个双方主动判断、12个双方沿用、54个一边主动一边沿用、2个至少一边等待确认，合计93个。双方沿用仅6/12标签一致，因少于24个月只报告计数、不显示比例；页面把13/25（52.0%）主动判断一致率置于主位，48/93（51.61%）含沿用标签一致率降为辅助口径。

## R5 执行记录（进行中）

- 120个月回测新增每进程8项 LRU 结果缓存、同键 single-flight 和深拷贝隔离。键由解析后的起止月、A1/A2/A3方法版本、模型代码及当前值、通用版本、合格独立发布证据三张表的水位组成；未分类或未核验记录不会触发无效冷算，无效的 `months` 参数也不会让相同实际区间重复冷算。
- 正常采集按版本写入后水位立即改变并自动失效；15分钟 TTL 为不遵循版本约定的原地维护提供陈旧上限。水位聚合使用短生命周期独立数据库会话，等待同键构建的请求不会长期占用连接。
- 真实库水位检查约0.426秒。A3 `1.1.0` 基线含敏感性的固定回放曾实测冷算313.654秒、缓存命中0.551秒；`1.2.0` 把独立证据水位纳入失效键，`1.3.0` 起只统计合格证据水位。本轮不把不同运行环境下的冷算耗时做直接性能比较。首个请求仍慢，多进程也会分别冷建；下一步补 `(indicator_code, date, version)` 等复合索引、月度中间结果预计算和跨进程并发限制，R5 尚未完成。

## A5 执行记录（进行中）

- A5a 已完成首轮前端解释：把四象限翻译为“偏弱但正在改善 / 偏强且继续改善 / 偏强但正在转弱 / 偏弱且继续转弱”，同时展示上一可比月，避免把缺数据或共同篮子变化月误当作真正转折。
- 贡献说明明确标注为模型内部的支撑与拖累，不宣称经济因果；“判断置信度”改为“本期证据质量”，并链接 A3 回测，让用户能把当期证据完整度与历史修订稳定性分开理解。
- A5b 已完成。A2 升级至 `1.1.0`：同步与领先面板均在原六个月共同篮子、原有效权重上逐项输出水平贡献和动能贡献；前者加总还原 `level_gap`，后者加总还原 `momentum_3m`，内部残差容差为 `1e-8`。无有效共同篮子时明确返回不可用，不把缺失当零。
- 每项驱动保留有效权重、近三月/前三月平均标准分及两侧来源观察期；前端展示完整同步分解、领先影响绝对值前五项，并注明加总仍使用全部信号。真实 2026-08 截面中，同步7项、领先12项的水平与动能残差均为0，加总校验全部通过。
- A5b 同时增加 `state_change`，把上一主动判断月、坐标轴变化、候选开始/累计/重置、阶段确认、死区继承和共同篮子暂停判定拆开陈述；数据完整但尚无历史方向可继承、且至少一个轴位于死区时单列为 `dead_zone_unclassified`，不再误报数据不足。它只是状态机的机械解释，不把模型贡献冒充现实因果。
- A5b 独立复核后增加回放性能保护：公开 A2 接口仍返回完整解释，A3 基线与固定调查内部回放显式关闭未消费的逐指标分解和状态说明生成，阶段状态机保持同一路径。新增测试覆盖固定四项调查的25%等权、缺一项即不可用、两轴残差校验，以及关闭解释载荷前后阶段路径一致。
- A5b 后续性能待办：120个月 A2 仍为每个历史月返回完整驱动清单，便于审计但响应约1.1MB，而当前详情页只消费最新月分解。后续应设计显式的紧凑响应或“仅最新月明细”参数，并先保留现有默认契约，避免静默删减历史审计字段。
- A5c 已完成首版。阶段翻转月可按需调用 `GET /api/analysis/cn/business-cycle/backtest/attribution?period=YYYY-MM`，构造实时可见信息 `R`、在完全相同实时输入支持集合上仅替换同键最终值的 `H`、完整最终信息 `F`。H 不承诺固定 A2 共同篮子：历史值修订会重算单边标准化、六个月共同篮子、有效权重与状态机，这些变化均属于该路径并单独审计。
- A5c 对同步水平差、同步动能、领先水平差、领先动能逐项验证 `R−F=(R−H)+(H−F)`；任一端缺失、同键最终值缺失/歧义、公式口径不匹配或加总失败时关闭数值解释，不把空值补零。阶段、已确认阶段和原始象限只报告每一步改变/未改变/不可比较，不分配原因百分比。
- A5c 采用单月惰性接口而不把第三套 A1/A2 重跑塞入120个月主回测：用户展开翻转月时才计算并保留本页结果。所有文案明确这是同端点伪实时重跑的顺序依赖模型审计，不是真实历史发布决策账本，也不是现实经济因果。
- A5c 单月结果另有独立的水位缓存、同键合并与15分钟 TTL，缓存键绑定 period、A1/A2/A3/A5c 方法版本及当前值/版本/合格证据三张表；页面内首次展开后也不会重复请求。实库 2022-09 首次完整 R/H/F 重跑约11.41秒，同水位缓存命中约0.15秒；2026-07 支撑预检约3.53秒，缓存命中约0.17秒。
- 真实 2026-07 曾因 current 财政脉冲缺 6 个同键最终值而返回 `hybrid_unavailable`；上述 missing-only 修复已解除该缺口。复验状态为 `available`：R/H/F 支撑数分别为 2,036/2,036/4,314，H 同键匹配 2,036，缺失、歧义和公式不匹配均为 0。R 与 H 均为收缩，F 为复苏；修订步骤不改变阶段，支持集合扩展步骤改变阶段，涉及 47 个修订输入和 2,278 个扩展输入。四项坐标差的加法恒等式全部通过且残差为 0，因此本次模型路径差异定位在 support expansion，而不是 revision step；这仍是顺序依赖的模型审计，不是现实经济因果归因。
- A5c 的月份入口已补充历史时区边界门：如 `1680-01` 这类会触发上海历史秒级偏移、无法安全表示为当前 RFC3339 响应的时点，会在查库前 fail-closed 并返回 HTTP 400，不再泄漏为 500；同时不武断设置现代年份下限，保留早于数据史时正常返回覆盖不足的既有语义。
